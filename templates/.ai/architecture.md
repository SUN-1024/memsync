# Architecture

## Stack

- Languages, frameworks, and runtimes used by this project.
- Major libraries that shape the codebase (not every transitive dependency).

If the stack is markdown-only or otherwise tooling-free, say so in one sentence.

## Repository layout

```
project-root/
├── ...                     # describe the top-level directories
└── ...                     # describe the entry points
```

Update this tree when directories are added, renamed, or removed.

## Components

### Component A
What it does, where it lives, and what depends on it.

### Component B
What it does, where it lives, and what depends on it.

## Data flow

Describe how a request, event, or task travels through the system. A short
ASCII diagram is often enough.

## Dependencies

- External services this project calls.
- Internal libraries this project consumes.
- Build tools required to develop the project.

If there are no runtime dependencies, say so in one sentence.

## Entry points

- For end users: which command, URL, or UI to start with.
- For maintainers: where to begin reading the code.

## Visible design decisions

Record decisions that future readers might otherwise have to rediscover. Each
entry should be a single sentence stating *what* was decided and *why*.
